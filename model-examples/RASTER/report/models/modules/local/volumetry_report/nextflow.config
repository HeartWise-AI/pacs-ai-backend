params {
    patient_name = false
    patient_id = false
}

process {
    withName: "VOLUMETRY_REPORT"{
        cpus = 1
        memory = { task.attempt * 4.GB }
        time = { task.attempt * 5.m }
        ext.patient_name = params.patient_name
        ext.patient_id = params.patient_id
    }
}
