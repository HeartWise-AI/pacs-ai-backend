docker.runOptions = '--entrypoint="" --gpus all -u 0:$(id -g) --shm-size=2g --rm'

process {
    withName: "STROKE_SEGMENTATION"{
        cpus = 4
        memory = { task.attempt * 8.GB }
        time = { task.attempt * 30.m }
    }
}
