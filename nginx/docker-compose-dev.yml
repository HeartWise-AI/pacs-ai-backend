networks:
  pacs-net:
    external: true

services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile-dev
    container_name: nginx
    restart: unless-stopped
    networks:
      - pacs-net
    ports:
      - 80:80
    depends_on:
      - orthanc
      - api-pacs
