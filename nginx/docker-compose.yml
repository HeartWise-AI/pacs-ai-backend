services:
  nginx:
    build: .
    container_name: nginx
    restart: unless-stopped
    env_file: .env
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - orthanc
      - api-pacs
